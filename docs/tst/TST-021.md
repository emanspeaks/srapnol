---
active: true
category: procedure
derived: false
level: 4.4
links:
- SRS-015: zswZ_C5mPTFK2f0zV879q9114qPgTkcPM7uHCY-jXS4=
- SRS-016: 7PKMM2XYA7E1YcmJR3OIP4wzsvBMJhhNRI1e_Lb3_nU=
- SRS-017: O2oWkNlGrJmuKVqedG_VG6Y6xbNF8XZFBtRQvaED2bA=
- SRS-062: qqfVARlZaDWplcRMsdKeDiYlWSoysbELXc2noTD1MO0=
normative: true
ref: ''
reviewed: 2kaEng_qFrmVlaOUlTCSR2yWaNxdGknuFj1XsL-lTEE=
status: pending
verification-method: Test
---

# Warm and basis starts

**Purpose.** Confirm re-solving from a solution with `.warm` and `.basis` converges immediately and that inconsistent hints do not break correctness.

**Grouping.** Requirements verification.

**Software version.** The version under test is recorded in the Software Test Report; this procedure applies from 0.1.0.

**Prerequisites.** Repository checked out at the version under test; toolchain and `rstd` per STP section 4.

**Steps.**

1. Run `zig build test` (toolchain per STP section 4).
2. Observe the `warm:` tests.

**Inputs.** The truss solved cold, then re-solved from its own `State` with `.warm` and with `.basis`; then re-solved with deliberately scrambled states.

**Expected results.** Warm and basis re-solves exit `optimal` within two major iterations; the scrambled re-solve still exits `optimal` at the same point.

**Pass/fail criteria.** Pass when every listed test reports success. A test that reports skipped counts as a pass only while every requirement it verifies has `status: pending` in SRS; otherwise the procedure fails.

**Rerun.** Repeatable without cleanup; the procedure is deterministic and has no external state.