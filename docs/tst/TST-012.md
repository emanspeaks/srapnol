---
active: true
category: procedure
derived: false
level: 3.3
links:
- SRS-001: -XDxHG1Bp2uuHC5Q0jRTSlOriXV0gLYUw9_2JBNqJyo=
- SRS-005: nbM_jonR1IS6Hf9tm9HsT4gI6qw0f7IK3Z4bFiKf35E=
- SRS-010: SnANMe8p_YP9zeNQRwtv3at9OHLyK5JQ3hzIYVu6SNs=
- SRS-072: NsoDF9iA5BlGFyroc2mrz2djRdfkKrj4VM6g1a47W7k=
normative: true
ref: ''
reviewed: VRRa-9K7yq6QM4fp4FyxQykkFuiC3bZKONb2q9Nlebs=
status: done
verification-method: Test
---

# Truss benchmark optimum

**Purpose.** Confirm the closed-form optimum reproduces the documented benchmark, is a KKT point with only `sigma3` binding and \(\lambda > 0\), and that the full \(F\) assembled through the `A`/`G` split matches it.

**Grouping.** Test-data validation; requirements verification.

**Software version.** The version under test is recorded in the Software Test Report; this procedure applies from 0.1.0.

**Prerequisites.** Repository checked out at the version under test; toolchain and `rstd` per STP section 4.

**Steps.**

1. Run `zig build test` (toolchain per STP section 4).
2. Observe `truss: documented optimum is the KKT point of the closed form`.

**Inputs.** `Truss.optimum()` and the numbers in `src/test/example.md` section 5.

**Expected results.** Benchmark values within their stated significant figures; closed forms to `1e-14`; stationarity to `1e-12`; `F` through the split to `1e-14`.

**Pass/fail criteria.** Pass when every listed test reports success. A test that reports skipped counts as a pass only while every requirement it verifies has `status: pending` in SRS; otherwise the procedure fails.

**Rerun.** Repeatable without cleanup; the procedure is deterministic and has no external state.

**Zig test names.**

- `truss: documented optimum is the KKT point of the closed form`