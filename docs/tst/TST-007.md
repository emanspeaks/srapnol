---
active: true
category: procedure
derived: false
level: 2.7
links:
- SRS-058: 7n4ONHyoM_jjhTOXMV0xW-6WVyuMuKu88aZnq7klLAA=
- SRS-086: cdjSOE0azr2b-CDa2wmFXm2aG4LLDly6Egq80fMi28o=
- SRS-087: 45CZ_TCLSuXtMwu7D5VVRKd3RfRo0gqPGiufflkJ4ps=
normative: true
ref: ''
reviewed: A3a2RUf8vgRo--Q4SOenhlYvU4M2Zc3BX3M-Al2Z9A4=
status: pending
verification-method: Test
---

# No allocation during solve

**Purpose.** Confirm `solve` performs no allocation after `init` and that `deinit` frees everything `init` allocated.

**Grouping.** Requirements verification.

**Software version.** The version under test is recorded in the Software Test Report; this procedure applies from 0.1.0.

**Prerequisites.** Repository checked out at the version under test; toolchain and `rstd` per STP section 4.

**Steps.**

1. Run `zig build test` (toolchain per STP section 4).
2. Observe the `allocation:` tests, which wrap `std.testing.allocator` in a failing allocator after `init` returns.

**Inputs.** The truss problem.

**Expected results.** `solve` completes with the failing allocator; the testing allocator reports no leak after `deinit`.

**Pass/fail criteria.** Pass when every listed test reports success. A test that reports skipped counts as a pass only while every requirement it verifies has `status: pending` in SRS; otherwise the procedure fails.

**Rerun.** Repeatable without cleanup; the procedure is deterministic and has no external state.