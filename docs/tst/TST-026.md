---
active: true
category: procedure
derived: false
level: 4.9
links:
- SRS-049: _5t_FwifCYwkLR8rG6xp_2cycof2xa1gGI3epe8hUR8=
- SRS-052: fffCUyxW_SaDAG9gmqTvg1-WQni4prrGFzT4WHMkFi4=
- SRS-055: JloQTSIneEMajGqu4JE8aqF8uvabJcr5x0m5W3KWjLs=
- SRS-056: VEgudaKP9ETZymYNtghEvri0xXRlPeCkDF4NMjwrDHY=
normative: true
ref: ''
reviewed: KVyHn2tLP_ETRZt5fsy5Ib9vR_92aZPaXgyo5d2P9UM=
status: pending
verification-method: Test
---

# Basis repair and ill-conditioning

**Purpose.** Confirm a singular basis is repaired by slack substitution and that a hopelessly ill-conditioned reduced Hessian exits rather than loops.

**Grouping.** Requirements verification.

**Software version.** The version under test is recorded in the Software Test Report; this procedure applies from 0.1.0.

**Prerequisites.** Repository checked out at the version under test; toolchain and `rstd` per STP section 4.

**Steps.**

1. Run `zig build test` (toolchain per STP section 4).
2. Observe the `basis:` tests.

**Inputs.** HS76 with a duplicated linear row (dependent rows force a singular initial basis); a two-variable problem whose objective is flat along a direction to machine precision.

**Expected results.** The duplicated-row problem solves to the HS76 optimum; the flat problem exits `ill_conditioned_null_space` within the iteration limit with finite `State`.

**Pass/fail criteria.** Pass when every listed test reports success. A test that reports skipped counts as a pass only while every requirement it verifies has `status: pending` in SRS; otherwise the procedure fails.

**Rerun.** Repeatable without cleanup; the procedure is deterministic and has no external state.