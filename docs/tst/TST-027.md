---
active: true
category: procedure
derived: false
level: '4.10'
links:
- SRS-046: Z8Lx3KRIHqUEQw-UNvVSW4RPPhbS5lvKxLfnGJv5s2s=
- SRS-093: oohGhIxiZmTNMXIQ44quaRM_nsLtO75q5vEj-NRmfuE=
normative: true
ref: ''
reviewed: YmGQwZfDDsDf-sxQAFFhgJzo3enjcwSkwjt3hWeZe3M=
status: pending
verification-method: Test
---

# Pattern estimation

**Purpose.** Confirm the `snJac`-equivalent routine recovers the `A` triplets and `G` pattern of a problem from its callback alone.

**Grouping.** Requirements verification.

**Software version.** The version under test is recorded in the Software Test Report; this procedure applies from 0.1.0.

**Prerequisites.** Repository checked out at the version under test; toolchain and `rstd` per STP section 4.

**Steps.**

1. Run `zig build test` (toolchain per STP section 4).
2. Observe the `pattern:` tests.

**Inputs.** The truss, HS47, and HS76 callbacks with their declared `A` and `G` withheld.

**Expected results.** Recovered `A` values match the declared ones within `function_precision` scaled by magnitude; the recovered `G` pattern equals the declared pattern; runs are deterministic (fixed seed).

**Pass/fail criteria.** Pass when every listed test reports success. A test that reports skipped counts as a pass only while every requirement it verifies has `status: pending` in SRS; otherwise the procedure fails.

**Rerun.** Repeatable without cleanup; the procedure is deterministic and has no external state.