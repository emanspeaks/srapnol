---
active: true
category: procedure
derived: false
level: 4.2
links:
- SRS-043: 8Bbo1f1BWCfwPbvvFY22DEHdFNG-Fkh14eNPJE43nWk=
- SRS-044: BCLguPcr3jvAVmN3XQBPb8o8kwogLKlVtFJ6TXlrgwA=
- SRS-054: WrWqWgpfdZYt2in9o0gWNC4thHk9pzKWPAtqs0-EY6Q=
normative: true
ref: ''
reviewed: 37Gx4AFcFKuQiDiFe2PT-6d2ammcDyoE2xl-zSom2nw=
status: pending
verification-method: Test
---

# Missing derivatives

**Purpose.** Confirm the solver estimates unset `G` entries by finite differences and still reaches the optimum.

**Grouping.** Requirements verification.

**Software version.** The version under test is recorded in the Software Test Report; this procedure applies from 0.1.0.

**Prerequisites.** Repository checked out at the version under test; toolchain and `rstd` per STP section 4.

**Steps.**

1. Run `zig build test` (toolchain per STP section 4).
2. Observe the `truss missing:` tests.

**Inputs.** The truss with selected `G` entries left NaN and `derivative_option = .some_missing`; then the same NaN with `.all_provided`.

**Expected results.** With `.some_missing` the optimum is reached within looser tolerances (`1e-4` relative); with `.all_provided` the exit is `incorrect_constraint_derivatives`.

**Pass/fail criteria.** Pass when every listed test reports success. A test that reports skipped counts as a pass only while every requirement it verifies has `status: pending` in SRS; otherwise the procedure fails.

**Rerun.** Repeatable without cleanup; the procedure is deterministic and has no external state.