---
active: true
category: procedure
derived: false
level: 2.8
links:
- SRS-064: ADQGXtIv08GOSeLx1EV_OdDKM031lWd5B4PCOKu0SAY=
- SRS-068: ew5qpqV3Y4YHWYDhyNF_sPZbSWCvDoCn33U9ws3iUxI=
normative: true
ref: ''
reviewed: 6IHDynWXi6fty9_-ZYUOtBNA_iBSXSE8FOa0qlfA7Mg=
status: pending
verification-method: Test
---

# Option validation

**Purpose.** Confirm `Solver.init` rejects invalid options with `error.InvalidOption` and accepts the defaults.

**Grouping.** Requirements verification.

**Software version.** The version under test is recorded in the Software Test Report; this procedure applies from 0.1.0.

**Prerequisites.** Repository checked out at the version under test; toolchain and `rstd` per STP section 4.

**Steps.**

1. Run `zig build test` (toolchain per STP section 4).
2. Observe the `options:` tests.

**Inputs.** Default options; then one invalid field at a time: a negative tolerance, `linesearch_tolerance = 1.0`, `major_iterations_limit = 0`, `hessian_updates = 0`.

**Expected results.** Defaults are accepted; each invalid field returns `error.InvalidOption` before any allocation or callback.

**Pass/fail criteria.** Pass when every listed test reports success. A test that reports skipped counts as a pass only while every requirement it verifies has `status: pending` in SRS; otherwise the procedure fails.

**Rerun.** Repeatable without cleanup; the procedure is deterministic and has no external state.