---
active: true
category: procedure
derived: false
level: 2.2
links:
- SRS-061: 0bDKmbZ3mB4-TSfKV5z-pPzLlsdg9VI8S2jFu-SFucQ=
- SRS-105: 0jH-QfJKpfeeB3wO8PDhFpTzuk0RZXo0xc7k-5Fs_iY=
normative: true
ref: ''
reviewed: OZzApwGkW49ezsfvPVe4pihhmogW0uNJn0KkPK9P_ZM=
status: done
verification-method: Test
---

# State construction

**Purpose.** Confirm `State.init` copies the starting point, zeroes states and multipliers, and rejects a starting point of the wrong length.

**Grouping.** Requirements verification.

**Software version.** The version under test is recorded in the Software Test Report; this procedure applies from 0.1.0.

**Prerequisites.** Repository checked out at the version under test; toolchain and `rstd` per STP section 4.

**Steps.**

1. Run `zig build test` (toolchain per STP section 4).
2. Observe `State.init copies x0 and zeroes everything else`.

**Inputs.** A 2-variable, 2-row problem and `x0 = (0.25, -0.5)`; then `x0` of length 1.

**Expected results.** `x` equals `x0`; `x_state` and `f_state` are `nonbasic_lower`; multipliers are zero; the short `x0` returns `error.LengthMismatch`.

**Pass/fail criteria.** Pass when every listed test reports success. A test that reports skipped counts as a pass only while every requirement it verifies has `status: pending` in SRS; otherwise the procedure fails.

**Rerun.** Repeatable without cleanup; the procedure is deterministic and has no external state.

**Zig test names.**

- `State.init copies x0 and zeroes everything else`