---
active: true
category: procedure
derived: false
level: 2.4
links:
- SRS-095: MtwnuEtea5QG3-6fHDls4gsjYjkcSX00ZiaYTea4pH8=
- SRS-098: pyiJUPL50vm-WAFHyZ0AjvhGm6ePeBSLDYi0JU-cVpM=
- SRS-101: rURP3uuDgNQ0wFgWJXg_uiSNYz_fuGXr-sai53TdDeA=
normative: true
ref: ''
reviewed: yy0-I04o6imqR1eFJmKXmm2axyZweE435yZ_4w_1WkU=
status: pending
verification-method: Test
---

# SIMD kernels against scalar twins

**Purpose.** Confirm every vectorized dense kernel agrees with its scalar reference implementation to rounding on the required lengths.

**Grouping.** Requirements verification; regression.

**Software version.** The version under test is recorded in the Software Test Report; this procedure applies from 0.1.0.

**Prerequisites.** Repository checked out at the version under test; toolchain and `rstd` per STP section 4.

**Steps.**

1. Run `zig build test` (toolchain per STP section 4).
2. Observe the `vec:` tests.

**Inputs.** Random-free, fixed vectors of length 0, 1, `w - 1`, `w`, `w + 1`, and 1001, `w` the lane width, for each kernel.

**Expected results.** Vector and scalar results agree within a few units in the last place.

**Pass/fail criteria.** Pass when every listed test reports success. A test that reports skipped counts as a pass only while every requirement it verifies has `status: pending` in SRS; otherwise the procedure fails.

**Rerun.** Repeatable without cleanup; the procedure is deterministic and has no external state.