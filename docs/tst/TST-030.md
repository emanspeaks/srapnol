---
active: true
category: procedure
derived: false
level: 5.2
links:
- SRS-094: dDDmt8gBLO8fzTKGTtJz4jiRzVzBmP4RPOK2qAcf428=
- SRS-106: 5PbItceNah-m4SgvhRwlrX7TVv04ThVyWDvfhEp6tOE=
- SRS-107: 8eJ-BuxDhMVGzGFKrAR5xTBBzCwg2zZpcRRSKX8CSwQ=
- SRS-110: SQuFoWnP8NqHnUaDnLZYMqiAMBYao-D1fnUW7xY6TlQ=
normative: true
ref: ''
reviewed: Lv7UBTgLKYyQg5fAAeCRn6r_O8Lv82Wt0ifr7U49_C8=
status: pending
verification-method: Demonstration
---

# Build and test matrix

**Purpose.** Demonstrate that the package builds and its procedures pass on every supported platform with the recorded toolchain, consumed as a Zig package.

**Grouping.** Demonstration; delivery.

**Software version.** The version under test is recorded in the Software Test Report; this procedure applies from 0.1.0.

**Prerequisites.** Repository checked out at the version under test; toolchain and `rstd` per STP section 4.

**Steps.**

1. Run `zig build test` (toolchain per STP section 4).
2. On each platform in the STP matrix: fetch the package into a scratch consumer's `build.zig.zon`, run `zig build` and `zig build test`; record toolchain version and results in the STR.

**Inputs.** The tagged release commit.

**Expected results.** Every platform builds and passes; the toolchain version matches the VDD.

**Pass/fail criteria.** Pass when every listed test reports success. A test that reports skipped counts as a pass only while every requirement it verifies has `status: pending` in SRS; otherwise the procedure fails.

**Rerun.** Repeatable without cleanup; the procedure is deterministic and has no external state.